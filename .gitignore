node_modules/
.vercel

# The Instagram export is the raw download; source/ is the normalised copy that
# actually gets committed, so the export itself is redundant in git.
export/

# Derived. Regenerated by `npm run build` on every deploy.
public/art/
public/list.html
